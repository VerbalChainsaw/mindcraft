# Capability needs

This is a supporting record of missing capabilities, not an architecture, plan,
handoff, authority, or work order. `AGENTS.md`, `ARCHITECTURE.md`,
`docs/CAMPAIGN-RECORD.md`, and `docs/HANDOFF.md` remain the complete repository
continuity path. Address an entry only when the selected tranche materially needs
it and the Director authorizes any required installation or mutation.

| ID | Status | Capability | Material need | Current effect |
|---|---|---|---|---|
| N-001 | `OPEN` | Canonical `center-multigeometry` skill | Read-only structural ownership mapping when a demonstrated failure crosses repository layers and the repair boundary remains unclear. | The skill is named by `AGENTS.md` but is unavailable in this session. Manual source mapping remains possible; this does not block the pending Phase 6 swim-exit probe. |
| N-002 | `OPEN` | Canonical `ladder-audit` skill | Read-only decomposition when one broad failed outcome has several plausible owners and a normal narrow investigation cannot isolate them. | The skill is named by `AGENTS.md` but is unavailable in this session. This does not block the pending Phase 6 swim-exit probe. |
| N-003 | `OPEN` | Read-only Codex OAuth subscription-capacity preflight | Show whether the authenticated ChatGPT subscription has sufficient remaining Codex capacity before starting a long unattended Luna matrix, without issuing a model request. | `codex login status` proves authentication but does not report remaining capacity. This does not block source work or provider-free physical probes; it is material before authorizing the corrected 112-cell Luna matrix. |
