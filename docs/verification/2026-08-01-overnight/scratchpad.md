# Overnight playability scratchpad

- Control plane: `http://[::1]:8080` (`127.0.0.1:8080` belongs to an unrelated tunnel client).
- Paper: managed Java 1.21.11 at `127.0.0.1:25579`; controlled fixture remains within `x=1026..1040`, `y=99..103`, `z=1006..1016`.
- Bot: `MindcraftBot`; held, idle, world-ready, and reset at `(1071.5,100,1007.5)`. Session-only autonomy is `command` so the pre-existing builder work order cannot compete; the saved profile remains unchanged.
- Original MIN-001 failure: candidate truncation hid supported targets, then natural collection had no stable-stance/post-break-support contract. A near rerun broke stone without collecting it; background retries later descended from Y=99 to Y=52 and drowned.
- Implemented owner-local repair: bounded hydrated candidate scan, safe drop-support and visible stance assessment, composite stance navigation that cannot break the target, execution-time revalidation, direct dig, and verified pickup.
- Terminal observability failure: a successful collection result was overwritten by the next job action; stop state could then wait for a heartbeat. Terminal/stop pushes now carry action identity and use immediate authoritative snapshots; lifecycle snapshots are delivered reliably across prior volatile movement gaps.
- Verifier contaminants found and removed: broadcast Paper markers, an old oak trunk, a target-matching stone floor, a natural stone just outside the lane, and target placement that allowed that outside stone to win legitimately. The outside stone at `(1037,100,1017)` was restored to its observed pre-run state.
- Final typed collection proof: three consecutive passes in 6925/7135/7031 ms; Paper inventory `0→3`; all targets `stone→air`; minimum Y=100; linked player action/decision on every run; stop quiescence 2/71/13 ms; ten seconds stable each.
- Core scenario 1 proof: three consecutive stopped→world-ready→`!stay(1)`→stopped runs. World-ready 6023/5986/6007 ms; structured action result 1512/1520/1515 ms after issue; graceful process shutdown 38/38/38 ms.
- Lifecycle verifier repairs were diagnostic-only: correlate `list` output after its newest command marker in the capped Paper log, and consume the current versioned state snapshot/delta protocol.
- Core scenario 4 proof: the certified traversal wall blocked the direct line at `(1074,100,1007)` while the adjacent opening at z=1006 remained clear. Three runs detoured through it at constant Y and reached the far target in 1499/1523/1538 ms; first progress 1052/1114/847 ms; Paper endpoint distance 0.142 each; stop 12/11/7 ms; no preemption or loop.
- Core scenario 7 proof: while operator-held, Paper provisioned hunger and then removed the Hunger effect before releasing the bot into command autonomy. Three deterministic survival-owned runs consumed exactly one cooked beef in 1594/1595/1597 ms; telemetry and Paper both observed hunger `12→20` and inventory `8→7`; linked survival decisions were retained on every run; stop quiescence was 0/1/2 ms and the bot remained held and motionless afterward. No production defect was found.
- COM-001 node/control evidence confirmed Pathfinder planned the diagonal `(1113.5,1060.5)→(1114.5,1061.5)` across the wall-end collision corner. The executor retained forward+sprint but stopped near `(1113.7,1060.7)`; bounded local recovery worked only by splitting that diagonal into cardinal clearance segments. Threat recognition, tactical selection, action ownership, support geometry, and damage attribution were all correct.
- The owner-local repair guards `safeMovements` diagonals with the executor's actual two-block collision envelope. A diagonal remains available in open space; when either orthogonal side corridor is blocked, A* emits executable cardinal steps instead. Supported line-of-sight melee stances and evidence-gated target/geometry replanning remain in the deterministic combat skill.
- Core scenario 6 typed proof is frozen: diagnostic build clear 3/3 and obstructed 3/3, followed by the instrumentation-free production build clear 3/3 and obstructed 3/3. Final obstructed durations were 4995/4975/4965 ms versus 14926/15531 ms before the corner repair; Paper damage was `0→200`, kills `0→1`, player decision linkage was present, stop quiescence was 5/2/7 ms, and cleanup restored held command-autonomy with no tagged target or wall. See `com-001-path-execution-trace.json`, `com-001-live-corner-guard.json`, `com-001-live-final.json`, and `com-001-resolution.md`.
- Natural-language defend acceptance is frozen. `fight the hostile` resolved deterministically to `!attackHostile`, whose structured terminal label was `action:attackHostile` while executing the same tactical combat skill. One clear and one initially hidden obstructed zombie both ended `succeeded:skill_secured`; Paper damage `0→200`, kills `0→1`, decision linkage, and cleanup all passed. The first verifier attempt was aborted before action because a Paper teleport was logged but one fresh canonical state remained at the previous coordinates for 15 seconds; an identical immediate rerun passed and the mismatch did not reproduce. Both artifacts are retained.
- Core scenario 8 proof: command-autonomy ran for 600638 ms with 583 canonical samples and 117 successful agent/Paper service polls. A persisted emergency-shelter order resumed once at startup, moved to its worksite, failed material preparation truthfully, placed one verified block during its bounded recovery, then terminally failed on `blueprint_incorrect_block` and stayed suppressed. The remaining session had no idle drift, action thrash, disconnect, crash, health/hunger loss, duplicate active owner, or false parent completion. Stop quiescence was 2 ms and held position remained stable for ten seconds. Paper independently confirmed final `(1032.4306,100,1012.6172)`, health 20, hunger 20, and on-ground state. See `session-001-live.json` and `session-001-paper-final.json`.
- Core scenario 5 is not complete until delivery to a real player or controlled player target is independently verified.
- Do not grant new arbiter authority during this work.
- FOL-001 reproduced a Pathfinder/direct-controller execution mismatch at a one-block rise: the bot was pressed against the ledge with `onGround=false`, while both raised diagonals and an unstaged forward jump intersected the landing corner. The owner-local repair rejects raised diagonals and stages cardinal ascents from a centered grounded stance before jumping.
- Follow is frozen on the instrumentation-free build. Three typed runs travelled 20.59/16.81/16.91 blocks through the two turns, doorway, and elevation change; stop quiescence was 187/190/189 ms. A separate stop-during-follow gate passed three times at 188/46/202 ms while the target continued moving, with ten-second stability after each stop. Natural-language `follow me` and `stop` passed against the same temporary controlled player. See `fol-001-live-staged-ascent-3x.json`, `fol-002-live-stop-3x.json`, `fol-003-live-natural-follow-isolated.json`, and `fol-004-live-natural-stop.json`.
- DEL-001 reproduced a truthful but unreliable inventory-window toss: at a 1.36-block stance the item passed the recipient and re-entered the bot's pickup envelope. The delivery owner now keeps at least 1.6 horizontal blocks of spacing, observes self-recollection, repositions, and retries at most three drops. Three typed runs passed with collection/delivery durations 2316/4758, 2286/4731, and 2324/4802 ms; Paper inventory was `0/0 -> 1/0 -> 0/1`; stop quiescence was 16/3/60 ms. See `del-001-live-post-repair-3x.json`.
- NL-001 exposed an existing ownership handoff gap: a natural item goal stopped the body but left a persisted work order active, so `GoalDirector.submit` truthfully rejected it as `job_busy`. Explicit player item goals now yield the body first and cancel only the stale work-order record before submission; automatic callers and ordinary one-active-order rejection are unchanged.
- Natural-language collect/deliver is frozen. `collect one cobblestone for me` and `bring me one cobblestone` both resolved to typed goals and the same deterministic skills. Final proof is `skill_collected` then `skill_delivered`, player-owned linked traces, Paper `0/0 -> 1/0 -> 0/1`, four independent collect events, 4 ms stop quiescence, ten-second stability, exact fixture restoration, cleared inventories, disconnected target, and `spawn_mobs` restored to its prior `true` value. See `del-002-live-natural-language-final.json`.
- Verifier-only corrections: use an explicit quantity in the natural delivery request so it cannot fall through to a model-invented mining job, and select the newest Paper begin marker when a run ID is reused. One post-reload precondition failure and one underspecified-phrase failure are retained as evidence; both cleanups restored the fixture and held the bot.
- Final checks: focused/directly adjacent 39/39; behavior 87/87 plus lint; critical 9/9 plus lint/format/syntax; full repository 420/420 in 84.3 seconds. The live bot remains world-ready and held, Paper remains running on port 25579, the controlled target is disconnected, and no fixture mutation remains.
- Every functional reset scenario has its required repeated proof and the monitored ten-minute session is clean. A literal interpretation that repeats the ten-minute duration gate three times would still require two additional no-change observation sessions; no known gameplay defect remains, and the local gameplay rules prohibit automatically expanding this into more soak work.
